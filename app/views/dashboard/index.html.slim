h1 = "Room \"#{@room.name}\" dashboard"

ul.user-cards.row
  - @users.each do |user|
    .col-sm-3
      .card.text-center
        .card-block
          h4.card-title = user.name
        ul.list-group.list-group-flush
          li.list-group-item Next on smth

h2 UserActions:
- @room.actions.each do |room_action|
  table.table.table-hover
    thead
      th.w-25 Name
      th.w-75 = "#{room_action.name.capitalize} progress"
    - @users.each do |user|
      tr
        td = user.name
        td
          .progress
            .progress-bar aria-valuemax="0" aria-valuemin="0" aria-valuenow="0" role="progressbar" style=("width: 39%")
